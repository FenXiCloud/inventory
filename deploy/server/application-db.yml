# production overrides — do NOT use redis URL with underscore hostnames
# (java.net.URI.getHost() returns null for contract_redis7)
spring:
  datasource:
    url: jdbc:mysql://contract_mysql8:3306/jxc?characterEncoding=UTF-8&useUnicode=true&useSSL=false&serverTimezone=GMT%2B8&allowMultiQueries=true&allowPublicKeyRetrieval=true
    username: jxc
    password: "@Flyemu891123"
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  sqltoy:
    debug: false
  data:
    redis:
      host: contract_redis7
      port: 6379
      # clear jar default url (underscore hosts break URI parsing)
      url: ""

app:
  upload-root: /upload

server:
  tomcat:
    basedir: /tmp

logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN
