-- 修复"以销定购"历史生成的"未审核"采购入库单：
-- 由于转采购生成的单据此前默认落入"未审核"状态，而采购入库单列表的
-- 编辑/审核/删除操作仅认"已保存"（草稿），导致这类单据无法操作只能批量删除。
-- 本脚本将其状态修正为"已保存"（草稿），恢复可编辑、可审核、可删除。
-- 幂等：可重复执行；仅影响以销定购来源且仍处于"未审核"的单据。

UPDATE jxc_purchase_inbound
SET order_status = '已保存'
WHERE source_type = '以销定购'
  AND order_status = '未审核';
